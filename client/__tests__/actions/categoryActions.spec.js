import expect from 'expect';
import categoryActions from '../../src/app/actions/categoryActions';

describe('>>>A C T I O N --- categoryActions', () => {
  describe('addCategorySuccess', () => {
    it('should create a ADD_CATEGORY_SUCCESS action', () => {
      const categoryInput = { name: 'My Category' };
      const expectedResult = {

      };
    });
  });
});
